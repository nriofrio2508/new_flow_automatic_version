name: Merge QA
on:
  workflow_dispatch:

jobs:
  merge_qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: qa
      
      - name: merge branch
        run: |
          echo "Current Branch: ${{ github.ref }}"
          git pull
          git merge ${{ github.ref }}
          git push origin qa
          echo "Finalizo merge"